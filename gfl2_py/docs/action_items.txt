GFL2 Parser — Action Items

   STATUS KEY:
      OPEN         - not started
      IN PROGRESS  - partially done
      DONE         - completed, kept for history

   This file tracks process/tooling follow-ups that are NOT bugs (see
   docs/known_issues.txt) and NOT settled design rationale (see
   docs/decisions.txt) — things a future session should pick up.  Filing
   something here under known_issues.txt would be misleading: nothing here
   is broken behavior in the shipped pipeline, it's work not yet done on
   the exploration/tooling side.

   NUMBERING: item numbers are not renumbered when an item is removed, but
   unlike known_issues.txt/decisions.txt this file does NOT keep tombstones
   for closed items -- action_items.txt is a scratch backlog (CLAUDE.md:
   "not a permanent record... trim... or delete it"), so a DONE or
   resolved item is simply deleted outright once its outcome is ported to
   known_issues.txt/decisions.txt (or, for pure process/tooling items, once
   the follow-up is complete).  Gaps in the numbering are expected and
   permanent; a new item still always takes max(existing)+1.


1.  Build a genuine held-out eval set for stat_ocr classifier experiments

   STATUS: IN PROGRESS -- core curation DONE (2026-07-01, see
   docs/decisions.txt #82 for the full held-out-set curation methodology
   and results); two follow-ups below remain open.

   REMAINING (low priority): stat_data.py's rare_dolls is scored against
   the 16-image held-out set itself, not the full ~87-image single/
   corpus -- a doll rare within the corpus but well-represented in this
   sample wouldn't be flagged.  Revisit if corpus-wide rarity is needed.

   NOTE: the eval set's ground truth itself was later found to contain
   13 wrong val-line labels (Tesseract mislabeling) -- see action item #6
   and docs/known_issues.txt §15.


5.  Replace tests/inputs/weekly_scores/ unit-test suite with an
    end-to-end integration test

   STATUS: OPEN

   CONTEXT: tests/test_score_pipeline.py + tests/inputs/weekly_scores/
   manifest.json test the weekly-score blob pipeline at the individual
   crop level — one pre-cropped digit-score image in, one expected string
   out, per manifest entry (reference.txt §5.4).  When stat_ocr2 was
   merged into stat_ocr.py, a regression slipped through undetected by
   this style of test; it was only caught by a full end-to-end run
   (equivalent in spirit to test_daily_gunsmoke.py's e2e test and
   test_weekly_gunsmoke.py's full-image parse tests).  Per-crop unit tests
   validate the classifier in isolation but don't exercise the
   surrounding pipeline wiring (crop extraction, panel/frame detection,
   module boundaries) where this regression actually lived.

   AGREED DIRECTION: for the weekly-score pipeline specifically, an
   end-to-end integration test (full image → full parsed output, same
   shape as test_weekly_gunsmoke.py's existing suite) is more valuable
   than the current per-crop manifest-driven unit tests at catching this
   class of regression.

   ACTION: add an end-to-end weekly-score integration test (full report
   image → parsed CSV rows, asserting score fields end to end) alongside
   or in place of the current tests/test_score_pipeline.py crop-level
   suite.  Decide during implementation whether the crop-level unit tests
   still pull their weight once the e2e test exists (e.g. keep them only
   for isolating which specific digit template regressed, drop them if
   the e2e test's failure output is diagnostic enough on its own).


6.  Systematically audit stat_ocr ground truth for other Tesseract-sourced
    mislabels; protect stat_data.py's manual corrections from silent clobber

   STATUS: OPEN

   RISK: stat_ocr ground truth is generated by re-running the blob
   pipeline itself (Tesseract only as a fallback when blob returns '?'),
   so it structurally cannot catch a case where blob is confidently
   wrong.  One instance of this (a '2'/'3' val-line mislabel) was found by
   incidental, non-systematic means -- see docs/known_issues.txt §15.
   There is no reason it's the only affected digit pair or column.

   ACTION (two related follow-ups, do either or both):
   - Systematic audit: rather than spot-checking one digit pair, script a
     broader cross-check (render every training/eval crop with its
     current label overlaid, or compare against an independently-sourced
     OCR pass, and scan for digit pairs with a suspiciously small
     combined-score gap in _classify()).
   - stat_data.py clobber protection: tests/generate_stat_inputs.py
     regenerates tests/inputs/daily/stat_data.py from scratch on every
     run with no override applied (reference.txt §5.3's CAUTION note) --
     a future re-run would silently discard the corrections tracked in
     stat_gt_overrides.json.  Wire stat_gt_overrides.json into
     generate_stat_inputs.py's collection step (mirroring the fix already
     applied to gfl2/stat_ocr.py's and gfl2/stat_ocr_padded.py's --build
     paths and to build_templates() itself).

   PROGRESS: one instance of the systematic audit (gfl2/stat_ocr_fft.py's
   pct-line '1'/'7' pair) was performed 2026-07-05 -- see docs/
   known_issues.txt §17's RESOLUTION for the full result (24 GT mislabels
   found and fixed).  gfl2/stat_ocr_padded.py has not been rebuilt with
   those overrides yet.  stat_data.py clobber protection is still
   unimplemented.

   PROGRESS (2026-07-12): a SECOND, distinct GT-corruption pattern found
   and fixed while investigating gfl2/stat_ocr_dp.py's remaining hard
   cases -- not a digit CONFUSION like every prior finding in this item,
   but Tesseract hallucinating an extra digit (always '1') with no
   corresponding ink at all, 7 instances, all pct-line, all adjacent to a
   '%' sign (plausibly misreading its diagonal slash).  Visually confirmed
   against the real rendered image for all 7 before correcting.  See
   docs/known_issues.txt §32 and decisions.txt #83.  Still not a
   scripted/systematic sweep -- found via targeted investigation of one
   engine's specific mismatch list, same "incidental, non-systematic
   means" caveat this item's RISK paragraph already flags.

   PROGRESS (2026-07-12): a THIRD, distinct GT-corruption pattern found
   while validating gfl2/header_ocr_dp.py's new classify_header() tree --
   this one on the header STATS ROW (dealt/taken/turns), a different
   corpus/collection path from every prior finding in this item (pct-line
   only, so far).  Not a digit confusion, and not a phantom extra digit
   with no ink either: this crop deliberately includes a sliver of the
   preceding label text ("...dealt"/"...taken"/"...turns") so the
   production classifier's own label-bleed-drop step can strip it -- but
   the Tesseract GT reads that SAME raw crop directly, and sometimes
   hallucinates the label's trailing letter as a leading digit ('s'->'5',
   'n'->'1').  100 of 101 whole-field mismatches fit this exact shape,
   visually confirmed on several real crops.  See docs/known_issues.txt
   §34 and decisions.txt #89, and docs/takeaways.txt #82 for the
   generalized lesson.  NOT fixed -- would need the GT collector to run
   through (or otherwise account for) the same label-bleed-drop logic the
   production classifier already applies, not just trust a whitelisted
   Tesseract call to ignore what it isn't supposed to read.  Still no
   scripted/systematic sweep across the whole codebase -- three
   incidental findings in three different engines/corpora is itself a
   signal this class of bug is common enough to warrant one.


12. assets/fonts/score_digits.py (the PRODUCTION score template) trains
    ONLY from Weekly Gunsmoke crops, never from a Daily Gunsmoke image

   STATUS: OPEN / Flagged, not investigated -- explicitly deferred, do not
           act on this without further direction. NARROWED 2026-07-12: the
           debug-atlas half of this concern (debugs/build_glyph_reference.py's
           glyph_daily_score.png) is now fixed -- see docs/decisions.txt #84.
           This item is now scoped to the PRODUCTION classifier template
           only.

   CONTEXT: assets/fonts/score_digits.py -- the actual shipped score-digit
   template -- is built only from tests/inputs/weekly_scores/manifest.json
   + its crop PNGs (the Weekly Gunsmoke score-digit corpus), via
   gfl2/score_ocr.py's build_templates() (fed by debugs/score_detect.py
   --build), never from a Daily Gunsmoke image. Yet
   gfl2/patterns/daily_gunsmoke.py imports that SAME score_digits.py DATA
   and the SAME THRESH_VAL/NORM_W/NORM_H constants from gfl2/score_ocr.py
   (see _read_bright_number and the medal-anchored score crop in
   _find_medal_right / around SCORE_X0) to classify Daily Gunsmoke's own
   header score field. So Daily Gunsmoke's score digits are currently
   classified against a font template that has never been built or
   validated from a single Daily Gunsmoke image.

   OPEN QUESTION (not investigated): is sharing one score-digit font
   across both report types intentional and safe (same underlying game
   UI element/rendering, so a separate Daily-specific training set would
   be redundant), or does Daily Gunsmoke's header score field render at a
   different scale/context that the weekly-only corpus doesn't actually
   cover -- meaning Daily Gunsmoke score reads have never really been
   accuracy-checked against their own native font instance? Whoever picks
   this up should check whether Daily Gunsmoke score misreads exist in
   tests/outputs/daily/ or the daily_gunsmoke*.js corpus before assuming
   either answer. NOTE (2026-07-12): decisions.txt #84's investigation
   found the real Daily corpus supports this directly -- scanning
   single/*.png's own medal-anchored score crops yields 23-95 real
   per-digit candidates, plenty to build and validate a genuine
   Daily-trained template if this item is picked up. That same
   investigation also surfaced one likely real Daily-specific
   blob-pipeline miss (single/fb_d_20251019.png panel 2, '4407' misread
   as '07' -- an adjacent '4','4' merge at SCORE_THRESH_VAL) worth
   checking first, since it may already answer the "do misreads exist"
   question above.


19. NORM_W_PCT/NORM_H_PCT (and other fixed-canvas constants) are
    hardcoded, not derived -- and measurably too small for the real corpus

   STATUS: OPEN

   CONTEXT: measured directly against the corpus: NORM_W_PCT=12 is too
   narrow (mean glyph width 12.8px, max 19px in the full extraction pass;
   77.2% of real pct-digit crops are wider than the 12px canvas) -- see
   docs/known_issues.txt §27 for the full measurement and independent
   corroboration from debugs/debug_sobel45_pct_glyphs.py /
   debug_sobel90_pct_glyphs.py, which size their own canvas fresh from the
   corpus and land on 15x20.

   ACTION: measure NORM_W_PCT/NORM_H_PCT (and NORM_W_VAL/NORM_H_VAL, the
   val-line equivalents, not yet checked the same way) directly from the
   corpus rather than continuing to hand-pick them, and re-validate every
   one of gfl2/stat_ocr.py's PRODUCTION templates and gfl2/
   stat_ocr_padded.py's after any change -- this constant is MAIN-scope,
   shared by every stat_ocr engine.  See action_items.txt #20 for the
   calibration/config mechanism this measurement should feed into.


20. Hardcoded, never-retrained calibration constants across gfl2/
    stat_ocr_fft.py make incremental improvement and new-font retraining
    tedious and error-prone -- build a real gfl2/calibration + gfl2/configs
    mechanism instead

   STATUS: IN PROGRESS -- scoped iteration DONE (see docs/decisions.txt
   #70 and #71 for the full mechanism (gfl2/calibration/, gfl2/configs/)
   and the corpus-driven vstroke_gate + Gabor kernel recalibration that
   closed nearly the whole §27 regression: 98.8% cell / 99.99% glyph,
   matching the pre-regression baseline); the deferred items below remain
   open.

   DEFERRED, explicit scope decisions (not oversights):
   - GABOR_MAX_C4/C7: dead, superseded by SOBEL_MAX_C4/C7 -- not worth
     calibrating.
   - Geometry constants (VSTROKE_KERNEL_H/W, HBAR_KERNEL_H/W, ink
     fractions, loop-ellipse geometry): never corpus-tuned at all -- would
     need new derivation methodology, not porting an existing one.
   - NORM_W_PCT/NORM_H_PCT sizing: tracked separately, see action item
     #19.


21. stat_ocr_fft: '2's bottom-band stroke-thickness measurement likely
    conflates curve extent with bar thickness, not yet investigated

   STATUS: OPEN

   CONTEXT: 2026-07-10, debugs/debug_line_stroke_thickness_sweep.py's
   corpus sweep (built to derive a physically-grounded stroke-thickness
   buffer for the LINE-SPLIT SOBEL MODE's '7' isolation gate, and to flag
   outlier images along two variance axes -- centroid shift and stroke
   thickness -- for the separately-requested top-20 meaningful-image
   curation). measure_stroke_thickness() counts consecutive rows within a
   digit-specific row band whose foreground occupancy clears BAR_INK_FRAC
   (0.5) of the glyph's width.

   ANOMALY: '7' and '5' (both straight top bars) measured a tight,
   consistent thickness across the whole 87-image corpus (3.696+/-0.46px
   and 3.678+/-0.467px respectively -- std under 0.5px either way). '2'
   (bottom band, 0.60-1.0 of height) measured 5.472+/-1.846px -- roughly
   4x the variance of the other two, despite all three using the exact
   same measurement mechanism and BAR_INK_FRAC threshold. SUSPECTED
   (NOT CONFIRMED): '2's bottom band captures its curved foot/tail, not a
   straight bar the way '7'/'5's top bands do -- a curve's extent through
   a fixed-height row band is a fundamentally different (and more
   variable) quantity than a straight bar's thickness, which would explain
   the inflated variance without it being a corpus-quality or measurement
   bug.

   NOT DONE: visually inspecting a handful of real '2' glyphs' bottom-band
   masks (same "unit debug before/alongside integration" convention this
   project uses elsewhere, e.g. debugs/debug_gabor_features.py) to confirm
   whether the row-band is genuinely catching a curved foot rather than a
   flat bar; if confirmed, '2's row band and/or the ink-fraction threshold
   likely need their own dedicated tuning rather than reusing '7'/'5's
   convention as-is. This was NOT blocking for the '7'-isolation gate work
   that motivated the sweep (which only needed '7's own measurement to be
   sound, and it independently validated cleanly -- see docs/decisions.txt
   for the full write-up) -- filed here so '2's number isn't mistaken for
   an equally-trustworthy stroke-thickness figure without this caveat.


23. Port ISO_GATE_LO/HI (the new isoperimetric-ratio root gate,
    docs/known_issues.txt §29, decisions.txt #73) into
    gfl2/calibration/calibrate_hierarchical.py's derivation pipeline

   STATUS: OPEN

   CONTEXT: ISO_GATE_LO/HI (0.48/0.95) currently ships as a hardcoded
   default in gfl2/stat_ocr_fft.py's _HIERARCHICAL_CALIB_DEFAULT --
   corpus-validated (found via debugs/debug_isoperimetric_hierarchy_check.py's
   Youden's J sweep over single/*.png, then hand-adjusted for real margin
   after a rounding bug, see decisions.txt #73), but not yet re-derivable
   through gfl2/calibration/calibrate_hierarchical.py the way
   PAREN_CLOSE_3_GATE/SOBEL_MEAN_C2/C5/SOBEL_MAX_C4/C7 already are. Same
   bootstrap position vstroke_gate was in before decision #70/#71 gave it
   a real derivation path.

   ACTION: this is an INTERVAL-style constant (like vstroke_gate), not a
   centroid -- per known_issues.txt §27's own lesson, it needs the real
   per-glyph corpus distribution (--images mode), not a single-atlas
   sample, to derive safely. Port debugs/debug_isoperimetric_hierarchy_check.py's
   sweep logic (or a safely-margined variant -- see takeaways.txt #65 for
   why the raw sweep optimum shouldn't be used directly) into
   calibrate_hierarchical.py as a new --images-only derivation, writing an
   "iso_gate" group to gfl2/configs/daily_pct_hierarchical_calib.json.


24. Add synthetic coverage for _classify_hierarchical()'s
    circular_holes0_unexpected defensive branch

   STATUS: OPEN

   CONTEXT: docs/known_issues.txt §29's new isoperimetric-ratio root gate
   routes any glyph the gate calls "circular" through inner-blob hole
   count next; holes==0 in that branch is unexpected (the gate measured
   recall=1.0000 on the real corpus, i.e. every true {0,6,8,9} glyph has
   holes>=1) and returns '?' rather than guessing. The real corpus never
   exercises this path (0 hits in every verify run so far) -- same
   category as HIERARCHICAL_BRANCH_NAMES' other defensive
   *_missing_centroid entries, which also see ~0 hits against a
   real, fully-trained corpus.

   ACTION: construct a synthetic glyph (e.g. a solid filled ellipse with
   no resolvable hole at this glyph resolution, or a real '0'/'6'/'8'/'9'
   crop with its hole artificially filled in) and confirm
   _classify_hierarchical() returns '?' via this branch rather than
   raising or silently misrouting -- a direct unit-level check rather
   than relying on the branch never firing in practice.


25. Test noncircular_mode="spread_y"/"gabor" robustness against a
    different font or a smaller/thinner glyph crop

   STATUS: OPEN

   CONTEXT: docs/known_issues.txt §30's promotion decision (docs/
   decisions.txt #74) argued gabor_45 is more likely than spread_y to
   break on a font/crop this corpus doesn't cover -- its (lambd, sigma,
   gamma) are tuned to THIS corpus's specific stroke width, with no
   font-independent derivation, and it already needed one real
   recalibration (decision #71) after §27's resize change. spread_y's own
   robustness to a different scale is ALSO untested, not assumed better
   -- the decision was about which risk is more likely to resurface, not
   a claim that spread_y is immune.

   ACTION: construct or source a glyph set at a meaningfully different
   scale (a different in-game font size, a downscaled/upscaled crop, or a
   synthetically thinned/thickened stroke) and run both
   --noncircular-mode gabor and --noncircular-mode spread_y against it,
   comparing accuracy degradation. Neither mechanism has been checked
   against anything but this project's single existing font/corpus.


28. gfl2/stat_ocr_dp.py's classify() tree has no confidence-based
    abstention -- it always answers, even at its narrowest real margin

   STATUS: OPEN -- flagged directly as a significant design gap.

   CONTEXT: every leaf in classify() always returns a specific digit; the
   only '?' path is one defensive branch the real corpus never hits.
   Unlike gfl2/stat_ocr.py (PROJ_CORR_MIN) and gfl2/stat_ocr_fft.py (two
   independently-gated agents), this engine has no notion of "the margin
   here is thin, don't guess."  Concretely: the '2'/'5' leaf's real corpus
   margin is exactly 1 unit, and synthetic perturbation testing found
   '2'/'5' failures dominate the engine's remaining fragility under
   stress, every one a CONFIDENT wrong answer (docs/takeaways.txt #69).

   ACTION: add a real margin/confidence check per leaf and return '?' when
   a glyph falls inside that margin, mirroring gfl2/stat_ocr.py and
   gfl2/stat_ocr_fft.py's existing discipline.  Likely more durable than
   further per-leaf recalibration.

   PARTIAL PROGRESS: one leaf (the {2,3,5} split) gained a reachable
   abstention path -- see docs/decisions.txt #81.  No other leaf has any
   confidence gate; the general ACTION above remains open.


29. gfl2/stat_ocr_dp.py's name ("dp" for cv2.approxPolyDP) is now
    misleading -- most leaves don't use approxPolyDP at all

   STATUS: OPEN -- flagged directly, not acted on (a rename touches every
           import site, including two debug scripts, and wasn't judged
           worth doing mid-session).

   CONTEXT: the module's own "WHY DP" docstring section explains the name
   -- the root mechanism was originally cv2.approxPolyDP-derived reflex-
   vertex spread (gfl2/stat_ocr_fft.py's known_issues.txt §30). After
   2026-07-11's updates (decisions.txt #76/#77), the tree is: isoperimetric
   ratio (root, contour geometry, no approxPolyDP), inner-blob hole count
   + paren/loop template correlation ('8'/{0,6,9} leaf, no approxPolyDP),
   proportional top-band ink counts ('4'/'7'/'2'/'5' leaves, no
   approxPolyDP) -- only the {1,7}-vs-{2,3,5} split still calls
   _reflex_vertices (approxPolyDP-based). The name now describes one leaf
   out of six, not the engine's defining mechanism.

   ACTION: not decided whether to rename. If picked up, needs updating
   the module file name, every internal reference, and both debug
   scripts that import from it (debugs/debug_stat_ocr_dp_5_vs_7_
   robustness.py, debugs/debug_stat_ocr_dp_topband_2v5.py) -- plus
   deciding whether "stat_ocr_dp" is worth keeping as a stable identifier
   regardless. A candidate alternative name wasn't proposed here.

   UPDATE (2026-07-11, decisions.txt #78): this module is now selectable
   via `main.py --stat-ocr-engine dp` -- no longer a pure EXPLORE-scope
   module with no production entry point, raising the stakes of a rename
   slightly (a real CLI value string, "dp", would also need to change,
   not just internal file/import references).


34. Add a lightweight periodic/automated check for stale docs/
    action_items.txt citations elsewhere in the repo

   STATUS: OPEN

   CONTEXT: two sessions now (docs/takeaways.txt #72 and #80) have found
   this same class of staleness by hand -- a citation to "docs/
   action_items.txt #N" surviving in code comments or other docs after
   item N closed (DONE, deleted, or superseded). Both times the fix was
   a manual, repo-wide grep-and-classify sweep across dozens of files;
   #80 additionally found that even the takeaways.txt entry ABOUT the
   prior cleanup carried a wrong cross-reference of its own, undetected
   for an unknown number of sessions. See docs/decisions.txt #88 for the
   full record of this session's sweep (~100 citations across 23 files).

   ACTION: not started. A candidate direction: a small script (or a step
   folded into the /land or /checkpoint skill) that (1) reads
   docs/action_items.txt and classifies every item as OPEN or not, (2)
   greps the repo for "action_items.txt #N" / "action item #N"
   citations, (3) flags any citing a non-OPEN number. Cheap to run on
   demand; would turn this from a multi-agent manual sweep into a
   one-command check, closing the gap before it reaccumulates across
   another dozen sessions.


35. No regression test coverage for gfl2/report.py's per-section stats
    or generate_report() itself

   STATUS: OPEN

   CONTEXT: gfl2/report.py (docs/decisions.txt #90) and the
   gfl2.patterns.daily_gunsmoke._record_field()/flush_section_stats()
   counters it reads were added and manually verified (a real 87-image
   folder run's generated report was inspected by hand against the
   corpus's already-documented fallback/'?' rates) but have no automated
   regression test -- unlike every other production-facing feature added
   this session's predecessors (e.g. test_score_ocr_dp_fixes_adjacent_
   digit_merge, test_header_ocr_dp_engine_runs_end_to_end).

   ACTION: add a test (likely tests/test_daily_gunsmoke.py or a new
   tests/test_report.py) that runs parse() on a small known fixture,
   calls flush_section_stats(), and asserts the returned counts (and/or
   a generate_report() call's resulting META/SECTIONS) match expected
   values -- so a future change to _record_field's call sites or
   generate_report()'s pipeline-span filtering can't silently drift
   without a test catching it.


36. No automated check that instrumented timer spans account for
    ~100% of a daily_gunsmoke run's own wall_clock_s

   STATUS: OPEN

   CONTEXT: docs/known_issues.txt §35 (decisions.txt #92, takeaways.txt
   #84) found real per-image work (cv2.imread, panel/frame detection,
   save_js) running completely outside any `timer.timed(...)` span --
   invisible to gfl2/report.py's SECTIONS and to the console's
   pipeline_summary() tree alike -- purely because a user manually summed
   the SECTIONS totals against META.wall_clock_s and noticed a gap. That
   gap was fixed (new `image_load`/`detect_panels`/`save_js` spans), but
   nothing stops a FUTURE untimed code path from reopening the same kind
   of silent gap -- there's no test that would fail if it did.

   ACTION: add a test (likely tests/test_daily_gunsmoke.py, alongside
   action_items.txt #35's proposed test_report.py coverage) that runs the
   real main.py folder/single path (or calls parse() + save_js the same
   way main.py does) on a small fixture, measures wall-clock the same way
   main.py does, and asserts `sum(r.ms for r in roots)` is within some
   small tolerance (a few ms, to allow for real Python/loop overhead) of
   the independently-measured wall-clock time -- so a future untimed code
   path shows up as a test failure instead of requiring another manual
   reconciliation of a generated report's numbers.
