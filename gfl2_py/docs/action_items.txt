GFL2 Parser — Action Items

   STATUS KEY:
      OPEN         - not started
      IN PROGRESS  - partially done
      DONE         - completed, kept for history

   This file tracks process/tooling follow-ups that are NOT bugs (see
   docs/known_issues.txt) and NOT settled design rationale (see
   docs/decisions.txt) — things a future session should pick up.  Filing
   something here under known_issues.txt would be misleading: nothing here
   is broken behavior in the shipped pipeline, it's work not yet done on
   the exploration/tooling side.


1.  Build a genuine held-out eval set for stat_ocr classifier experiments

   STATUS: DONE (2026-07-01 — held-out image set curated; debugs/pct_fft_predict.py
           now trains/evaluates on a genuine split; see RESULT below)

   debugs/pct_fft_predict.py (§15 exploration, added 2026-07-01) originally
   built FFT+Gabor centroids from ALL glyphs in single/*.png and then
   classified the SAME glyphs (train==test).  Both the console summary and
   the generated tests/inputs/daily/pct_fft_ground_truth.py carried an
   explicit caveat that the 99.4%-on-answered accuracy was a best-case
   separability check, not a generalization estimate.

   DONE (2026-07-01):
   - Audited the old 20-image tests/inputs/daily/stat.json set against
     doll-frame coverage: it only exercised 17/30 dolls in assets/dolls/,
     and contained several exact-duplicate doll rosters (e.g. 4 ib_d_
     images and 6 fb_d_/gm_d_ images sharing an identical 8-10 doll set —
     same challenge period, no new visual information beyond digit values).
   - Dropped 9 of the most redundant duplicates (keeping the
     highest-hard-cell-count representative per duplicate roster) and
     added 5 replacement images (gm_d_20260316, gm_d_20260202,
     gm_d_20250908, ib_d_20260114, gm_d_20260111) chosen by greedy set
     cover over the missing dolls.  Net: 16 images, all 30 dolls covered,
     19 hard ('?') cells retained/gained, gm_/fb_/ib_ prefixes stay
     balanced (6/6/4).
   - Replaced tests/inputs/daily/stat.json with tests/inputs/daily/
     stat_data.py (see docs/decisions.txt-style rationale: this repo's
     JSON->Python module pattern, precedented by assets/fonts/*.py).  The
     module exports CROPS (unchanged ground-truth shape) and a new META
     dict per image: dolls present, rare_dolls (doll appears in <= 2 of
     the selected images), hard_cells, km_suffix_cells,
     structural_outlier (docs/known_issues.txt cross-ref), and an additive
     importance_score for quick ranking.
   - tests/generate_stat_inputs.py now computes and writes this metadata
     automatically (via a disabled-side-effects call into
     daily_gunsmoke.parse() for doll names) every time it's re-run, so the
     eval set stays self-describing as images are swapped in the future.
   - Updated tests/test_stat_ocr_hard_cases.py and
     tests/test_stat_ocr_padded_explore.py to load stat_data.py instead of
     stat.json; refreshed test_stat_ocr_padded_explore.py's
     _KNOWN_FAILING xfail set (dropped stale entries for removed images,
     added the same known '3'->'2' val-line confusion found on the new
     ib_d_20260114.png — see docs/known_issues.txt §15).
   - Full suite green: 1273 passed, 14 skipped, 13 xfailed.

   DONE (2026-07-01, continued) — debugs/pct_fft_predict.py wired to the split:
   - held_out_image_paths() resolves tests/inputs/daily/stat_data.py's CROPS
     keys against single/ — that's the fixed evaluation set.
   - --train (default "single/*.png") selects the training pool; any image
     also present in the held-out set is excluded from training regardless
     of the glob, so there is no leakage path via a careless --train value.
   - Centroids are built ONLY from the training pool; classification and the
     printed accuracy run ONLY on the held-out set.  pct_fft_ground_truth.py's
     docstring and DATA now describe/contain the held-out predictions only,
     with the training-image count recorded for provenance.

   RESULT (single/*.png, 71 train / 16 held-out images, 2026-07-01):
      digits classified   : 1982
      answered            : 1257  (63.4%)
      flagged '?'         : 725  (36.6%)
      accuracy on answered: 1254/1257  (99.8%)
   This is a genuine generalization estimate (centroids never saw the
   held-out images) and lands close to the old train==test number (99.4%),
   which is a good sign the FFT+Gabor features aren't just memorizing —
   but note the two numbers aren't measuring the same thing, so "close" is
   reassuring, not a formal comparison. Total wall time ~33s for all 87
   single/*.png images (well under the "should be fast" expectation for a
   corpus this size).

   REMAINING (not done in this pass, low priority):
   - rare_dolls in stat_data.py's META is scored against the 16-image set
     itself, not the full single/ corpus (~87 images) — cheap to compute,
     but a doll rare within the corpus that happens to be well-represented
     in this particular 16-image sample would not be flagged.  Acceptable
     for now; revisit if corpus-wide rarity is needed for a future
     selection.


2.  Run reports should carry an explicit, labelled timestamp

   STATUS: DONE (2026-07-02)

   Generated reports (debugs/pct_fft_predict.py's console summary and its
   tests/inputs/daily/pct_fft_ground_truth.py output, debugs/score_detect.py's
   report_*.txt, gfl2.stat_ocr --verify output, etc.) did not record when
   they were produced.  Without a timestamp, a stale report generated
   before a template rebuild was indistinguishable from a fresh one.

   CONVENTION CHOSEN: run START (when the script began), not run END.
   Rationale: the stated use case — telling whether a report predates a
   template/asset rebuild — is best served by comparing against that
   file's mtime, and START is simpler to capture once and thread through
   to every report-writing function without re-querying the clock per
   report.  Documented once in reference.txt §5.5 rather than per-script.

   IMPLEMENTED:
   - debugs/score_detect.py: run_start captured at top of main(), printed,
     and passed into _write_report()/_write_summary() as the first line
     of report_tesseract.txt, report_blob.txt, report_summary.txt.
   - gfl2/stat_ocr.py verify(): run_start captured on entry, printed as
     the first line of the --verify console summary block.
   - debugs/pct_fft_predict.py: run_start captured at top of main(),
     printed to console, and written into the docstring header of the
     generated tests/inputs/daily/pct_fft_ground_truth.py.
   - Format: ISO 8601 local time via datetime.now().isoformat(seconds),
     labelled "Generated: <timestamp>  (run start)" in every case.


3.  Document how to read OCR run logs/reports (PSM modes, fallback tiers)

   STATUS: OPEN

   Run output references implementation details without explanation: e.g.
   "--psm 6" / "--psm 4" retry in _collect_cells._tess_label, the "--psm 11"
   date OCR fallback (known_issues.txt §3), and the blob → Hu → Tesseract
   fallback chain with '?' markers for low-confidence cells.  Someone
   reading a log without Tesseract/pipeline context has no way to tell
   whether a given line is expected behavior or a red flag.

   ACTION: add a short glossary section — in reference.txt or a new
   docs/reading_the_logs.txt — covering: what each PSM mode means and when
   it's tried, what triggers each fallback tier (blob → Hu → Tesseract, and
   the pct/val '?' convention), and what a healthy vs. concerning log looks
   like.


4.  Weekly Gunsmoke pipeline writes output CSVs into tests/inputs/

   STATUS: OPEN

   FIRST OBSERVED: three stray, untracked CSVs found in
   tests/inputs/weekly_gunsmoke/ (gm_250730.csv, gm_250801.csv,
   gm_250818.csv) sitting next to the tracked .png test fixtures of the
   same name, during a 2026-07-01 untracked-files audit.

   ROOT CAUSE: main.py's weekly_gunsmoke output defaults to "<image>.csv
   in the same folder as the input image" (reference.txt §1.1) when
   --output is not given.  That convention is fine for production input
   folders (single/, or a user's own screenshot folder), but when someone
   runs main.py directly against a fixture image under tests/inputs/ for
   manual inspection, the default silently writes the result back into
   the test-fixtures directory — mixing generated output with committed
   input, and leaving dangling CSVs behind if not cleaned up by hand.

   ACTION: modify the weekly Gunsmoke pipeline so output never lands under
   tests/inputs/ by default.  Candidate approaches (pick one):
   - Detect when image_path resolves under tests/inputs/ and default
     output under tests/outputs/weekly_gunsmoke/ instead (mirrors the
     existing tests/outputs/daily/ convention used by daily Gunsmoke).
   - Or: change the default entirely to always require --output for
     anything outside a designated "production input" root, forcing the
     caller to be explicit rather than relying on folder-sniffing.
   Whichever approach is chosen, document it in reference.txt §1.1 next to
   the existing "Output file: defaults to <image_name>.csv" line.


5.  Replace tests/inputs/weekly_scores/ unit-test suite with an
    end-to-end integration test

   STATUS: OPEN

   CONTEXT: tests/test_score_pipeline.py + tests/inputs/weekly_scores/
   manifest.json test the weekly-score blob pipeline at the individual
   crop level — one pre-cropped digit-score image in, one expected string
   out, per manifest entry (reference.txt §5.4).  When stat_ocr2 was
   merged into stat_ocr.py, a regression slipped through undetected by
   this style of test; it was only caught by a full end-to-end run
   (equivalent in spirit to test_daily_gunsmoke.py's e2e test and
   test_weekly_gunsmoke.py's full-image parse tests).  Per-crop unit tests
   validate the classifier in isolation but don't exercise the
   surrounding pipeline wiring (crop extraction, panel/frame detection,
   module boundaries) where this regression actually lived.

   AGREED DIRECTION: for the weekly-score pipeline specifically, an
   end-to-end integration test (full image → full parsed output, same
   shape as test_weekly_gunsmoke.py's existing suite) is more valuable
   than the current per-crop manifest-driven unit tests at catching this
   class of regression.

   ACTION: add an end-to-end weekly-score integration test (full report
   image → parsed CSV rows, asserting score fields end to end) alongside
   or in place of the current tests/test_score_pipeline.py crop-level
   suite.  Decide during implementation whether the crop-level unit tests
   still pull their weight once the e2e test exists (e.g. keep them only
   for isolating which specific digit template regressed, drop them if
   the e2e test's failure output is diagnostic enough on its own).
